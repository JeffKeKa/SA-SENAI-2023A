import { StatusBar } from "expo-status-bar";
import { StyleSheet, View, Text, Image, TouchableOpacity } from "react-native";
import Cadastro from './src/Views/LOGIN33';

export default function App() {
    return( 
        <View>
        <Cadastro/>
        </View>
    )
}

const style = StyleSheet.create({}    
);